# Vercel Blob (required for /api/products to read/write `products.json` in Blob storage)
# Dashboard: Project → Storage → Create Blob store → connect to this project.
# Then either run `vercel env pull` in this repo, or paste the token here as `.env.local`.
BLOB_READ_WRITE_TOKEN=
